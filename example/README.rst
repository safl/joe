An adhoc example of using cijoe
===============================

The ``env.yaml`` contains the environment definition.
The ``workflow.yaml`` contains the workflow

The folder ``worklets`` contains misc. examples of worklets.
